#!/bin/zsh

function main() {
    local class=$1
    local package=$(android.meta-dat @package)
    if [[ "$class[1]" == "." ]]; then
        echo $package$class
    else
        echo $class
    fi    
}

main $*
